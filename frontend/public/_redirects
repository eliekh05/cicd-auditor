# Cloudflare Pages _redirects
# Place this file at: frontend/public/_redirects
# It will be copied into dist/ during build.

# API requests → Worker (set VITE_WORKER_URL in Pages env vars)
/api/*  https://pipeline-scout-production.up.railway.app/api/:splat  200

# SPA fallback — all unmatched routes serve index.html
/*  /index.html  200
